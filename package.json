{
  "name": "flare-song-db",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy": "astro build && wrangler pages deploy",
    "ingest": "node scripts/ingest.mjs",
    "ingest:originals": "node scripts/ingest-originals.mjs",
    "ingest:covers": "node scripts/ingest-covers.mjs",
    "ingest:seed": "node scripts/ingest-seed.mjs",
    "enrich:durations": "node scripts/enrich-durations.mjs"
  },
  "dependencies": {
    "astro": "^5.5.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^5.9.3"
  }
}
